{
  "name": "pawify-api",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "tsc -p tsconfig.test.json && node --test \"lib-test/**/*.test.js\"",
    "test:emulator": "tsc -p tsconfig.test.json && firebase emulators:exec --only auth,database,firestore --project pawify-test \"node --test lib-test/test/emulator/**/*.test.js\"",
    "start": "node --enable-source-maps lib/index.js",
    "dev": "ts-node src/index.ts"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@sentry/node": "^10.53.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.8",
    "firebase-tools": "^15.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "private": true
}